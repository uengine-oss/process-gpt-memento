{
  "_comment": [
    "요청에 실을 샘플링 파라미터. providers 는 프로바이더 기본값, models 는 모델별 규칙이다.",
    "우선순위: models[].locked > 호출부가 넘긴 값 > models[] 기본값 > providers[] 기본값.",
    "locked 는 모델이 강제하는 값이다 — 어기면 400 이므로 호출부보다 우선한다.",
    "max_tokens_field 는 출력 길이 파라미터의 이름. 모델마다 다르다.",
    "models 의 키는 모델명 앞부분과 맞춰보며, 가장 긴(구체적인) 규칙이 이긴다.",
    "top_p·presence_penalty·frequency_penalty 는 OpenAI 호환 1급 필드로,",
    "나머지(top_k·min_p·repetition_penalty·chat_template_kwargs)는 vLLM 확장이라",
    "extra_body 로 전달한다.",
    "enable_thinking=false 가 핵심이다 — 켜두면 'ok' 한 마디에도 사고 토큰을 뱉어",
    "출력이 19배(2→38 토큰) 늘고 카드 생성처럼 호출이 많은 작업이 타임아웃한다."
  ],
  "providers": {
    "custom": {
      "temperature": 0.7,
      "top_p": 0.8,
      "presence_penalty": 1.5,
      "extra_body": {
        "top_k": 20,
        "min_p": 0.0,
        "repetition_penalty": 1.0,
        "chat_template_kwargs": {
          "enable_thinking": false
        }
      }
    }
  },
  "models": {
    "gpt-5": {
      "locked": {
        "temperature": 1
      },
      "max_tokens_field": "max_completion_tokens"
    },
    "o1": {
      "locked": {
        "temperature": 1
      },
      "max_tokens_field": "max_completion_tokens"
    },
    "o3": {
      "locked": {
        "temperature": 1
      },
      "max_tokens_field": "max_completion_tokens"
    },
    "o4": {
      "locked": {
        "temperature": 1
      },
      "max_tokens_field": "max_completion_tokens"
    }
  }
}
